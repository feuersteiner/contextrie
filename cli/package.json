{
  "name": "contextrie-cli",
  "version": "0.1.1",
  "description": "macOS arm64 CLI binary for Contextrie.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "bin": {
    "contextrie": "dist/contextrie"
  },
  "os": [
    "darwin"
  ],
  "cpu": [
    "arm64"
  ],
  "files": [
    "dist/contextrie",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/feuersteiner/contextrie.git",
    "directory": "cli"
  },
  "homepage": "https://github.com/feuersteiner/contextrie/tree/main/cli",
  "bugs": {
    "url": "https://github.com/feuersteiner/contextrie/issues"
  },
  "keywords": [
    "contextrie",
    "cli",
    "agents",
    "context"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "bun run ./index.ts",
    "build": "bun build ./index.ts --compile --target=bun-darwin-arm64 --outfile ./dist/contextrie",
    "build:darwin-arm64": "bun run build",
    "prepack": "bun run typecheck && bun run build"
  },
  "devDependencies": {
    "@ai-sdk/openai": "^2.0.0",
    "@contextrie/core": "file:../core",
    "@contextrie/parsers": "file:../parsers",
    "@types/bun": "latest",
    "@types/node": "^24.8.1",
    "meow": "^13.2.0",
    "typescript": "^5"
  }
}
